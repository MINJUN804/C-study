#include <stdio.h>
#define _CRT_SECURE_NO_WARNINGS

/*
int main(void)
{
	int time;
	int hour, min, sec;
	printf("time(sec):");
	scanf("%d", &time);

	hour = time / 60;
	min = time / (60^2);
	sec = time % (60^4);

	printf("hour:%d min:%d sec:%d \n", hour, min, sec);
	return 0;


}
*/